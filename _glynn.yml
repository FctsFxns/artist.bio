# Glynn setup

ftp_host: 'artist.bio'
ftp_dir: '/public_html'
ftp_passive: true

# optional
ftp_port: 21                  # default 21
ftp_secure: false             # default false

# credentials
# On travis via environment variables